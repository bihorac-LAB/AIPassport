# AI Passport Secrets Template
# Copy this to secrets.toml and fill in the values

# Required for the AI Guide and the LLM-backed notebook activities
# (1.1 Fact-or-Fiction, and all of Module 7).
#
# This is a UF NaviGator Toolkit key. The app talks to the NaviGator endpoint in
# aipassport_config.NAVIGATOR_TOOLKIT_BASE_URL through the OpenAI-compatible SDK.
NAVIGATOR_TOOLKIT_API_KEY = "your-navigator-toolkit-key-here"
