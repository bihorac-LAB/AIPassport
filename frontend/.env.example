# Base URL of the FastAPI backend. This is the ONLY variable the browser bundle receives.
# Never put a database URL, an LLM key, a JWT secret, or SMTP credentials here — Vite inlines
# every VITE_* variable into the published JavaScript.
VITE_API_BASE_URL=http://127.0.0.1:8000
