[pytest]
testpaths = tests
asyncio_mode = auto
# Both the fixtures and the tests must share one event loop: the async engine and its
# connection pool are session-scoped, and asyncpg connections cannot cross loops.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
