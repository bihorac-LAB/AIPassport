{
  "id": "4.1_clinical",
  "module": "Module 4 - Machine Learning",
  "subsection": "4.1",
  "microskill": "4.1",
  "track": "clinical",
  "title": "Building a Model End to End",
  "audience": "Clinical researchers and clinician-scientists new to AI",
  "objectives": [
    "Run a supervised pipeline end to end on one shared split.",
    "Read a decision tree's rules and trace a single prediction through them.",
    "Explain what scaling, a 1-D convolution, and dropout actually do.",
    "Choose a classification threshold deliberately rather than accepting 0.5.",
    "Report cross-validated performance as a spread rather than one number."
  ],
  "sections": [
    {
      "name": "Tab 1 - Data, Split, and Scaling",
      "purpose": "Establish the one split every later tab uses.",
      "how_to_use": "Preview rows with the slider, inspect .dtypes, plot a feature by outcome, set the test-set size (stratified split, random_state=42), and toggle StandardScaler on one row to see why scaling matters for gradient-based models and not for trees."
    },
    {
      "name": "Tab 2 - A Model You Can Read",
      "purpose": "Train a decision tree and trace one prediction through it.",
      "how_to_use": "Set max depth and watch test vs. training accuracy separate. Read the plotted tree or its text export, then move the eight feature sliders to get a live class and probability."
    },
    {
      "name": "Tab 3 - A Model With More Capacity",
      "purpose": "See what a dense network buys and what it costs.",
      "how_to_use": "Three mechanism visualisers: the dropout neuron grid, the Conv1D sliding kernel, and the Keras architecture blocks. Then set epochs and batch size, and move the classification threshold to trade sensitivity against specificity. These metrics come from the network's real 5-fold held-out probabilities, not a formula."
    },
    {
      "name": "Tab 4 - Validating and Choosing",
      "purpose": "Report a spread rather than one number, then decide what to ship.",
      "how_to_use": "Set the fold count for cross-validation and read the four averaged metrics plus the per-fold bar chart. Then use the priority select_slider (Interpretability / Balanced / Performance) for a recommendation."
    }
  ],
  "chatbot_guidance": [
    "The dataset is the bundled Pima Indians Diabetes dataset (768 records, 8 numeric features, binary Outcome). Earlier versions of this notebook described it as eICU in-hospital mortality; that was wrong and is corrected.",
    "Tabs 2-4 all use the split made in tab 1. If a learner changes the test size, every later number changes with it.",
    "Conv1D is deliberately shown as the wrong tool for this dataset: these columns have no natural order. It is the right tool for an ECG trace.",
    "Accuracy alone is misleading here -- 500 of 768 records are negative, so always predicting 'negative' scores about 65%."
  ]
}
